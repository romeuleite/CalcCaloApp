package com.example.projetofinal

//Interface para passar a referência do fragment que implementa o ButtonClickListener para o Adapter

interface ButtonClickListener {
    fun onButtonClickListener(number: Double)
}